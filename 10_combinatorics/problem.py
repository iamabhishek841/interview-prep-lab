"""Given queries (n,r) and a prime modulus with max n < modulus, return
C(n,r) mod modulus for each query. For r<0 or r>n return 0. Precompute once.
"""
def combination_queries(queries: list[tuple[int, int]], modulus: int) -> list[int]:
    raise NotImplementedError
