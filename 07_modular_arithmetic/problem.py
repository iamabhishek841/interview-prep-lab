"""For each (numerator, denominator, modulus), return numerator/denominator
mod modulus, or None if the denominator has no modular inverse. modulus>1.
Use extended Euclid, not brute-force inverse search.
"""
def modular_division_queries(queries: list[tuple[int, int, int]]) -> list[int | None]:
    raise NotImplementedError
