"""Return, for each query (left, right, a, b), the count of integers x in
[left, right] divisible by a or b. Constraints: 0 <= left <= right <= 10**18;
a,b > 0. Do not iterate through the interval.
"""

def count_divisible_queries(queries: list[tuple[int, int, int, int]]) -> list[int]:
    raise NotImplementedError
