"""Return True when n is prime and remains prime after every repeated left
truncation and every repeated right truncation. n must be positive and cannot
contain digit zero. Use an O(sqrt(x)) primality test; do not sieve to n.
"""
def is_two_sided_truncatable_prime(n: int) -> bool:
    raise NotImplementedError
