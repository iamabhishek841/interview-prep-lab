"""For inclusive non-negative ranges, return prime counts. Build one sieve to
the maximum right endpoint and answer each query with prefix counts.
"""
def prime_count_queries(ranges: list[tuple[int, int]]) -> list[int]:
    raise NotImplementedError
