"""Given non-empty positive process periods, return (first_sync_time, cycles),
where first_sync_time is their LCM and cycles[i] is first_sync_time//periods[i].
Do not modify the input and do not search timestamps incrementally.
"""
def synchronisation_plan(periods: list[int]) -> tuple[int, list[int]]:
    raise NotImplementedError
